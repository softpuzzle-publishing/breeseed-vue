import CommonHelper from "@/utils/ui_helper/common_helper";

class ProductItemHelper extends CommonHelper {
  constructor(id) {
    super(id);
  }

  setItem(item) {
    return item;
  }
}

export default ProductItemHelper;
