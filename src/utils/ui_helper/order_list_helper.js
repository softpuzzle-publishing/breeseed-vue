import DataHelper from "@/utils/ui_helper/data_helper";

class OrderListHelper extends DataHelper {
  constructor({ type, group }) {
    super({ type, group });
  }
}

export default OrderListHelper;
