import CommonHelper from "@/utils/ui_helper/common_helper";

class ProductItemsListHelper extends CommonHelper {
  constructor(id) {
    super(id);
  }

  setItems(items) {
    return items;
  }
}

export default ProductItemsListHelper;
