import DataHelper from "@/utils/ui_helper/data_helper";

class BoardImgListHelper extends DataHelper {
  constructor({ type = "board-img-list", group }) {
    super({ type, group });
  }
}

export default BoardImgListHelper;
