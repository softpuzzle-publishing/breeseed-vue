import CommonHelper from "@/utils/ui_helper/common_helper";

class StateIconListHelper extends CommonHelper {
  constructor(id) {
    super(id);
  }

  setItems(items) {
    return items;
  }
}

export default StateIconListHelper;
