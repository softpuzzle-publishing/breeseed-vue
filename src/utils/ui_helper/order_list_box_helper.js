import DataHelper from "@/utils/ui_helper/data_helper";

class OrderListBoxHelper extends DataHelper {
  constructor({ type, group }) {
    super({ type, group });
  }
}

export default OrderListBoxHelper;
